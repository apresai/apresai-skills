# Node: score

Only if 5 or more findings were raised. One batched LOOKUP-tier (haiku)
agent that did not write them. Keep at 80 or above. Burden is on keeping.
Print `Filtered: N raised, M dropped below 80`. A dropped CRITICAL stays
listed as `[dropped: <score>, <=10-word reason]`.

Below 5 findings, the parent filters itself with the same 80 rule.
