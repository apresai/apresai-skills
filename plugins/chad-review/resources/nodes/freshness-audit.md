# Node: freshness-audit

```bash
bash "${CLAUDE_PLUGIN_ROOT:-$HOME/.claude/skills/chad-review}/resources/freshness.sh"
```

Do not update. Judgment of REF polarity, SCAN none, N/A, and COVERAGE GAP
is in `commands/chad-review.md` § 5 FRESHNESS. Read that section when this
node runs. Prefer `FIX` records over raw `SCAN` lines.
