# Node: contract-mirror

```bash
bash "${CLAUDE_PLUGIN_ROOT:-$HOME/.claude/skills/chad-review}/resources/contract-mirror.sh"
```

`MIRROR` is HIGH, CRITICAL if the diff touched either side. `STALECOMMENT` is
HIGH when it names regeneration debt. `SUMMARY gen_files=0` is
`N/A - convention not detected`.
